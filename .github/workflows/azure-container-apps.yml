# 使用.NET 8 来部署 Azure Container Apps，以确保兼容性和稳定性。
# 原生 .NET 部署 (Oryx)，不使用 DOCKER
# 变量全部手动配置在ACA中，不再通过GitHub Secrets传递

name: Deploy .NET 8 to Azure Container Apps

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# ===================== 关键点 1：删除或注释掉 permissions =====================
# 如果保留 id-token: write，Action 会强制尝试 OIDC，导致你看到的错误。
# permissions:
#   id-token: write
#   contents: read
# ===========================================================================

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/Saas.Infra.Net.sln

      - name: Build project
        run: dotnet build src/Saas.Infra.Net.sln --configuration Release

      - name: Publish project
        run: dotnet publish src/Saas.Infra.Net.sln --configuration Release --output ./publish

      # ===================== 关键点 2：纯净的密钥登录 =====================
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SAASWEBAPP3_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SAASWEBAPP3_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SAASWEBAPP3_AZURE_SUBSCRIPTION_ID }}
          client-secret: ${{ secrets.SAASWEBAPP_AZURE_CLIENT_SECRET }}
          # 移除 enable-AzPSSession 等干扰参数，保持最基础的 4 项凭据
      # =================================================================

      - name: Deploy to ACA
        uses: azure/container-apps-deploy-action@v1
        with:
          appName: saas-web-app3
          resourceGroup: saas-infra-group
          source: ./publish
          targetPort: 8080
          ingress: external
          environmentVariables: |
            ASPNETCORE_ENVIRONMENT=Production
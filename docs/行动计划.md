# 5天MVP保命计划（单人版｜优先保演示）
**核心原则**：能跑通 > 架构优雅；能演示 > 功能完整；规避卡点 > 死磕技术
**时间**：2026.3.2-2026.3.6
**更新说明**：
1. 已验证 MiniAuth+JWT+Swagger 集成成功，移除 MiniAuth 卡点应对；
2. 新增 Stripe 卡点替代方案（本地模拟支付）；
3. 新增数据库极简设计+实施计划，融入原有日程；
4. 核心目标不变：保「登录→支付（模拟）」演示流程。

## 前置风险规避（3.1 下午，2小时）
| 任务 | 操作 | 目的 |
|------|------|------|
| MiniAuth 验证完成 | 确认 MiniAuth+JWT 「生成Token→验证Token」流程跑通，无需切换方案 | 锁定认证方案，避免Day1卡壳 |
| Stripe 卡点应对 | 1. 尝试注册Stripe失败（海外地址限制）；<br>2. 备用方案：开发「模拟支付接口」，前端保留Stripe样式，后端返回「支付成功」模拟数据；<br>3. 保存Stripe官方测试卡号（4242 4242 4242 4242），预留后续切换真实接口的空间 | 规避Stripe注册卡点，保证支付流程能演示 |
| 数据库预准备 | 1. 确定使用SQLite（无需安装，文件存储，零部署成本）；<br>2. 新建空数据库文件，配置EF Core基础依赖 | 提前规避数据库配置卡点 |

---

## 2026.3.2（Day1）：认证落地 + 数据库极简设计（全天）
### 核心目标：跑通JWT登录鉴权 + 完成数据库极简设计&实施
| 优先级 | 任务 | 操作 | 简化点/卡点应对 |
|--------|------|------|----------|
| ✅ | 认证方案落地（MiniAuth+JWT） | 1. 集成MiniAuth+JWT到MVC项目；<br>2. 写死测试用户（admin/123456），同时接入数据库验证（双方案兜底）；<br>3. 实现登录接口 `/api/auth/login` + 极简登录页 | 数据库仅做「用户查询」，不做注册/改密 |
| ✅ | 数据库极简设计&实施 | 1. 设计2张核心表：<br>   - **Users表**（用户表）：<br>     - Id: int（主键，自增）<br>     - Username: varchar(50)（唯一，非空）<br>     - Password: varchar(50)（非空，MVP阶段明文存储）<br>     - CreateTime: datetime（默认当前时间）<br>   - **PaymentLogs表**（支付日志表）：<br>     - Id: int（主键，自增）<br>     - UserId: int（关联Users.Id）<br>     - Amount: decimal(10,2)（支付金额）<br>     - Status: varchar(20)（支付状态：Success/Fail）<br>     - CreateTime: datetime（默认当前时间）<br>2. 用EF Core Code First生成SQLite数据库；<br>3. 插入测试数据：Users(1, 'admin', '123456', '2026-03-02') | 放弃密码加密、用户角色、订单关联等，只保核心字段 |
| ✅ | 鉴权验证 | 1. 加`[Authorize]`特性到测试接口；<br>2. 验证：无Token拒绝访问，有Token正常访问；<br>3. 验证：数据库查询用户 vs 写死用户均能生成Token | 只验证核心逻辑，不做复杂权限 |

### 今日底线：
1. 能从数据库查询用户并生成JWT Token；
2. 鉴权接口能正常拦截/放行请求；
3. SQLite数据库生成成功，测试数据插入正常。

---

## 2026.3.3（Day2）：认证收尾 + 支付基础（上午认证，下午支付）
### 上午：认证收尾（2小时）
- 登录页优化：Token存入LocalStorage，登录成功跳转首页；
- 验证数据库稳定性：重启项目后，仍能查询用户生成Token；
- 只做「登录→鉴权」闭环，注册/改密/登出全砍。

### 下午：支付基础接入（4小时）
| 任务 | 操作 | Stripe卡点应对 |
|------|------|----------------|
| 支付接口开发 | 1. 安装Stripe.NET SDK（预留真实接口）；<br>2. 开发「模拟支付接口」`/api/payment/create`：<br>   - 接收参数：Amount（金额）、UserId（从Token解析）；<br>   - 写入PaymentLogs表（Status默认Success）；<br>   - 返回模拟数据：{ "clientSecret": "mock_secret_123", "status": "Success" } | 放弃真实Stripe调用，用模拟接口兜底 |
| 数据库联调 | 验证支付接口能正常写入PaymentLogs表，重启项目后数据不丢失 | 只验证写入功能，不做查询/统计 |

### 今日底线：
1. 登录后能调用支付接口；
2. 支付接口能写入数据库，返回模拟的clientSecret。

---

## 2026.3.4（Day3）：Stripe 支付页 + 流程打通（全天）
### 核心目标：能唤起「模拟Stripe支付弹窗」，能完成测试支付
| 任务 | 操作 | 简化点 |
|------|------|--------|
| 支付页开发 | 1. 新建支付页（仅登录后可访问）；<br>2. 页面元素：金额输入框 + 支付按钮；<br>3. 引入Stripe.js（前端样式），调用 `/api/payment/create` 接口；<br>4. 弹窗提示「支付成功」（模拟Stripe支付弹窗） | 不做样式美化、表单验证，能调接口就行 |
| 支付流程验证 | 1. 输入金额（如10元），点击支付按钮；<br>2. 验证PaymentLogs表中新增记录；<br>3. 前端跳转「支付成功页」 | 放弃真实支付回调，用前端跳转代替 |

### 今日底线：
1. 登录→支付页→调用支付接口→写入数据库→前端提示成功；
2. 完整流程无阻断性Bug。

---

## 2026.3.5（Day4）：流程整合 + 问题修复（全天）
- 打通全流程：登录 → 支付页 → 发起支付 → 成功页；
- 数据库问题修复：确保用户表/支付日志表读写正常，无数据丢失；
- 只修复「阻断流程」的Bug（如Token失效、支付接口写入失败）；
- 页面只保留核心按钮/文字，不做任何美化；
- 不写文档、不画架构图、不整理代码。

### 今日底线：
1. 端到端流程能跑通（登录→支付→成功）；
2. 数据库能正常读写，无阻断性Bug。

---

## 2026.3.6（Day5）：部署Azure + 演示准备（全天）
### 上午：部署（4小时）
1. 发布MVC项目到Azure App Service；
2. 配置环境变量（JWT密钥、Stripe API Key（预留）、SQLite数据库路径）；
3. 开启HTTPS，测试线上登录/支付流程；
4. 验证线上数据库（SQLite文件）能正常读写。

### 下午：演示准备（2小时）
1. 写30秒演示脚本：<br>   - 步骤1：输入admin/123456登录；<br>   - 步骤2：跳转到支付页，输入金额10元；<br>   - 步骤3：点击支付，弹窗提示成功；<br>   - 步骤4：展示数据库中新增的支付记录（可选）；
2. 准备测试账号，反复测2遍；
3. 放弃所有非核心优化，只保演示流畅。

### 今日底线：
1. 线上地址能正常访问；
2. 演示流程流畅无卡顿；
3. 数据库数据正常持久化。

---

# 关键保命策略（必看）
## 1. 数据库风险控制
- 全程使用SQLite，避免Azure SQL的配置/费用/连接问题；
- 数据库仅做「最小化读写」：用户查询、支付日志写入，不做复杂操作；
- 本地测试通过后再部署，避免线上数据库卡点。

## 2. Stripe卡点应对
- 彻底放弃真实Stripe调用，用「模拟接口+前端样式」完成演示；
- 保留Stripe SDK和接口参数，后续注册成功后可快速切换为真实接口。

## 3. 工作量控制
- 每天只做「底线任务」，完成即停，不额外加功能；
- 数据库字段只保留核心，放弃所有非必要字段（如用户邮箱、支付备注）；
- 密码暂用明文存储，MVP演示后再优化加密。

## 4. 时间分配
- 认证+数据库（Day1+Day2上午）：合计1.5天；
- 支付（Day2下午+Day3）：合计1.5天；
- 整合+部署（Day4+Day5）：合计2天。

---

# 最终交付物（只保这4样）
1. 线上可访问地址：能登录、能发起模拟支付；
2. 测试账号：admin/123456；
3. 30秒演示脚本：能流畅演示核心流程；
4. SQLite数据库文件：包含用户数据和支付日志（可选展示）。

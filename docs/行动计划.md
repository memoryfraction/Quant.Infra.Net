# 5天MVP保命计划（单人版｜优先保演示）
**核心原则**：能跑通 > 架构优雅；能演示 > 功能完整；规避卡点 > 死磕技术
**时间**：2026.3.2-2026.3.6

## 前置风险规避（3.1 下午，2小时）
| 任务 | 操作 | 目的 |
|------|------|------|
| MiniAuth 预验证 | 1. 新建空MVC项目，尝试集成MiniAuth+JWT；<br>2. 跑通「生成Token→验证Token」最简流程；<br>3. 若30分钟卡壳，立刻切换为「ASP.NET Core Identity + JWT」 | 提前暴露MiniAuth卡点，避免Day1/2卡壳 |
| Stripe 预验证 | 1. 用Stripe官方.NET示例，跑通「创建PaymentIntent」；<br>2. 保存测试卡号（4242 4242 4242 4242） | 确认Stripe SDK能正常调用 |

---

## 2026.3.2（Day1）：只搞定「认证」（全天）
### 核心目标：不管用MiniAuth还是替代方案，必须跑通JWT登录鉴权
| 优先级 | 任务 | 操作 | 卡点应对 |
|--------|------|------|----------|
| ✅ | 认证方案落地 | 1. 若MiniAuth无卡点：集成MiniAuth+JWT；<br>2. 若MiniAuth卡壳：直接用ASP.NET Core JWT（放弃MiniAuth）；<br>3. 写死测试用户（admin/123456），不连数据库；<br>4. 实现登录接口 `/api/auth/login` + 极简登录页 | 放弃MiniAuth不丢人，MVP只看「能登录、能鉴权」 |
| ✅ | 鉴权验证 | 1. 加`[Authorize]`特性到测试接口；<br>2. 验证：无Token拒绝访问，有Token正常访问 | 只验证核心逻辑，不做复杂权限 |

### 今日底线：必须能拿到JWT Token，能鉴权（哪怕放弃MiniAuth）

---

## 2026.3.3（Day2）：认证收尾 + Stripe 基础（半天认证，半天支付）
### 上午：认证收尾（2小时）
- 登录页优化：Token存入LocalStorage，登录成功跳转首页
- 只做「登录→鉴权」闭环，其他全砍（注册/改密/登出都不做）

### 下午：Stripe 基础接入（4小时）
- 安装Stripe.NET SDK，配置API Key
- 实现 `/api/payment/create` 接口：只接收金额，返回客户端Secret
- 不做回调、不做订单、不存数据，只保证能生成Secret

### 今日底线：Stripe能生成PaymentIntent Secret

---

## 2026.3.4（Day3）：Stripe 支付页 + 流程打通（全天）
### 核心目标：能唤起Stripe支付弹窗，能测试付款
| 任务 | 操作 | 简化点 |
|------|------|--------|
| 支付页开发 | 1. 新建支付页，仅保留「金额输入框 + 支付按钮」；<br>2. 引入Stripe.js，复制官方示例代码；<br>3. 登录后才能进入支付页 | 不做样式、不做表单验证，能调接口就行 |
| 支付测试 | 1. 用Stripe测试卡号完成付款；<br>2. 不处理Webhook回调，仅前端提示「支付成功」 | 放弃服务端回调，用前端跳转代替 |

### 今日底线：能完成测试支付，前端能看到「支付成功」

---

## 2026.3.5（Day4）：流程整合 + 问题修复（全天）
- 打通全流程：登录 → 支付页 → 支付 → 成功页
- 只修复「阻断流程」的Bug（比如Token失效、支付接口报错）
- 页面只保留核心按钮/文字，不做任何美化
- 不写文档、不画架构图、不整理代码

### 今日底线：端到端流程能跑通，无阻断性Bug

---

## 2026.3.6（Day5）：部署Azure + 演示准备（全天）
### 上午：部署（4小时）
- 发布MVC项目到Azure App Service
- 配置环境变量（JWT密钥、Stripe API Key）
- 开启HTTPS，测试线上登录/支付流程

### 下午：演示准备（2小时）
- 写30秒演示脚本（登录→支付→成功）
- 准备测试账号，反复测2遍
- 放弃所有非核心优化，只保演示流畅

### 今日底线：线上能演示完整流程

---

# 关键保命策略（必看）
1. **MiniAuth卡点应对**：
   - 若集成MiniAuth超过3小时没进展，立刻切换为「纯JWT」（ASP.NET Core自带），MVP阶段「能登录」比「用指定组件」更重要；
   - 记录MiniAuth卡点，后续迭代再解决，不卡MVP交付。
2. **工作量控制**：
   - 每天只做「底线任务」，完成即停，不额外加功能；
   - 所有需要连数据库/写复杂逻辑的操作，全部用「写死数据」替代。
3. **时间分配**：
   - 认证（Day1+Day2上午）：合计1.5天（应对MiniAuth卡点）；
   - 支付（Day2下午+Day3）：合计1.5天；
   - 整合+部署（Day4+Day5）：合计2天。

---

# 最终交付物（只保这3样）
1. 线上可访问地址：能登录、能支付；
2. 测试账号：admin/123456；
3. 30秒演示脚本：能流畅演示核心流程。
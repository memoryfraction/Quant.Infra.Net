# 5天MVP交付执行计划（2026.3.2-2026.3.6）
**核心目标**：单人完成「Stripe支付 + JWT SSO」MVP，基于`Saas.Infra.Net`三合一MVC架构部署至Azure Serverless
**优先级说明**：✅ 必须完成｜🔶 可选优化｜❌ 直接砍掉（MVP阶段不做）

---

## 前置准备（2026.3.1 18:00前，启动前最后准备）
| 任务 | 具体操作 | 交付物 |
|------|----------|--------|
| 环境初始化 | 1. 拉取最新代码，确认`src`下6个项目引用正常；<br>2. 申请Stripe测试账号，获取`Secret Key`/`Publishable Key`/`Webhook Secret`；<br>3. 生成32位JWT密钥（随机字符串）；<br>4. 安装NuGet包：`System.IdentityModel.Tokens.Jwt`、`Stripe.net`、`Microsoft.AspNetCore.Authentication.JwtBearer` | Stripe密钥配置表、JWT密钥文本 |
| 工具准备 | 1. VS Code打开`docs`文件夹，创建日志文档；<br>2. 打开Mermaid Live Editor（https://mermaid.live/）；<br>3. Azure控制台创建空的App Service（Serverless模式） | 工具就绪，Azure空服务配置截图 |

---

## 2026.3.2（Day 1）：基础骨架搭建
**核心目标**：完成项目依赖调整，封装JWT/Stripe通用基础能力，打通类库与MVC引用链路

### 上午（09:00-12:00）：类库层核心封装
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | Core层JWT工具封装 | 1. 在`Saas.Infra.Core`新建`Auth/Jwt`文件夹；<br>2. 编写`JwtOptions.cs`（Issuer/Audience/SecretKey/过期时间）；<br>3. 编写`JwtHelper.cs`（生成Token、验证Token静态方法） | `src/Saas.Infra.Core/Auth/Jwt/` | 本地调用`JwtHelper.GenerateToken`能生成有效JWT |
| ✅ | SSO层业务接口定义 | 1. 在`Saas.Infra.SSO`新建`Abstractions`文件夹；<br>2. 定义`IAuthService.cs`（含`LoginAsync`方法）；<br>3. 新建`Models`文件夹，添加`LoginRequest.cs`/`LoginResponse.cs` | `src/Saas.Infra.SSO/Abstractions/`、`src/Saas.Infra.SSO/Models/` | 接口编译通过，模型字段完整 |
| ✅ | Payment层Stripe基础配置 | 1. 在`Saas.Infra.Payment`新建`Stripe`文件夹；<br>2. 编写`StripeOptions.cs`（ApiKey/WebhookSecret）；<br>3. 编写`StripeClientExtensions.cs`（注册Stripe客户端） | `src/Saas.Infra.Payment/Stripe/` | Stripe客户端初始化无密钥错误 |

### 下午（14:00-18:00）：MVC层基础整合与配置
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | 项目引用调整 | 1. `Saas.Infra.MVC`引用`SSO/Payment/Data`类库；<br>2. 确保所有类库引用`Saas.Infra.Core` | 项目依赖配置 | 所有项目编译无引用错误 |
| ✅ | 配置文件编写 | 1. `Saas.Infra.MVC`的`appsettings.json`添加JWT/Stripe节点；<br>2. `Program.cs`注入JwtOptions/StripeOptions | `src/Saas.Infra.MVC/appsettings.json`、`src/Saas.Infra.MVC/Program.cs` | 配置注入无报错 |
| 🔶 | 数据库极简配置 | 1. `Saas.Infra.Data`搭建SQLite（本地文件）；<br>2. 新增`User`表（Id/UserName/PasswordHash） | `src/Saas.Infra.Data/Context/` | 能手动插入测试用户数据 |

### 当日总结
- 完成标记：□ 是 □ 否
- 问题记录：_______
- 明日调整：_______

---

## 2026.3.3（Day 2）：JWT SSO核心实现
**核心目标**：完成SSO业务层实现，MVC对接登录接口，实现「登录→颁发JWT→接口鉴权」闭环

### 上午（09:00-12:00）：SSO业务层与数据层实现
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | 数据层用户查询实现 | 1. `Saas.Infra.Data`新建`Repositories`文件夹；<br>2. 编写`UserRepository.cs`（`GetUserByUserNameAsync`方法） | `src/Saas.Infra.Data/Repositories/` | 能查询到手动插入的测试用户 |
| ✅ | SSO业务层登录实现 | 1. `Saas.Infra.SSO`新建`Services`文件夹；<br>2. 编写`AuthService.cs`（实现`IAuthService`，密码校验+生成Token）；<br>3. 数据库添加测试用户（admin/123456） | `src/Saas.Infra.SSO/Services/` | 传入正确账号密码能生成JWT |

### 下午（14:00-18:00）：MVC层鉴权配置与登录接口/页面
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | MVC鉴权中间件配置 | 1. `Program.cs`添加JWT认证/授权中间件；<br>2. 配置`[Authorize]`默认策略 | `src/Saas.Infra.MVC/Program.cs` | 未携带Token访问受保护接口返回401 |
| ✅ | 登录API接口编写 | 1. `Saas.Infra.MVC`新建`Controllers/AuthController.cs`；<br>2. 编写`POST /api/auth/login`接口，调用`IAuthService`完成登录 | `src/Saas.Infra.MVC/Controllers/AuthController.cs` | Postman调用接口能返回JWT |
| ✅ | 登录页面编写 | 1. 新建`Views/Auth/Login.cshtml`；<br>2. 编写极简表单，AJAX调用登录接口，Token存入LocalStorage | `src/Saas.Infra.MVC/Views/Auth/Login.cshtml` | 页面输入账号密码能获取并存储Token |

### 当日总结
- 完成标记：□ 是 □ 否
- 问题记录：_______
- 明日调整：_______

---

## 2026.3.4（Day 3）：Stripe支付核心实现
**核心目标**：完成Stripe支付Intent创建、前端支付对接、支付结果回调处理

### 上午（09:00-12:00）：Payment业务层实现
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | 支付Intent创建实现 | 1. `Saas.Infra.Payment/Stripe`新建`StripePaymentService.cs`；<br>2. 编写`CreatePaymentIntentAsync`（接收金额/用户ID，返回客户端Secret） | `src/Saas.Infra.Payment/Stripe/` | 调用方法能返回有效客户端Secret |
| ✅ | 支付回调处理实现 | 1. 编写`HandleWebhookAsync`（验证签名，处理支付成功/失败事件）；<br>2. 极简记录支付结果（写入本地日志） | `src/Saas.Infra.Payment/Stripe/` | 能解析Stripe回调数据并记录日志 |

### 下午（14:00-18:00）：MVC层支付接口/页面与鉴权关联
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | 支付API接口编写 | 1. 新建`Controllers/PaymentController.cs`，加`[Authorize]`；<br>2. 编写`POST /api/payment/create`（调用支付服务）、`POST /api/payment/webhook`（接收Stripe回调） | `src/Saas.Infra.MVC/Controllers/PaymentController.cs` | 携带Token调用创建接口能返回客户端Secret |
| ✅ | 支付页面编写 | 1. 新建`Views/Payment/Checkout.cshtml`；<br>2. 引入Stripe.js，通过客户端Secret唤起支付弹窗；<br>3. 登录后才能进入支付页 | `src/Saas.Infra.MVC/Views/Payment/Checkout.cshtml` | 能唤起Stripe测试支付弹窗 |
| 🔶 | Stripe Webhook本地调试 | 使用Stripe CLI转发回调到本地`/api/payment/webhook` | 本地调试环境 | 能接收并处理支付成功回调 |

### 当日总结
- 完成标记：□ 是 □ 否
- 问题记录：_______
- 明日调整：_______

---

## 2026.3.5（Day 4）：MVC一体化整合与文档
**核心目标**：打通登录→支付→结果展示全流程，完成文档编写

### 上午（09:00-12:00）：流程整合与体验优化
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | 页面跳转优化 | 1. 登录成功后自动跳转到支付页；<br>2. 支付完成后跳转到极简结果页 | `src/Saas.Infra.MVC/Views/`、`Controllers/` | 端到端流程无卡顿 |
| ✅ | 错误处理极简实现 | 1. 登录/支付接口添加异常捕获；<br>2. 前端用alert提示错误 | 全项目相关文件 | 接口报错返回友好信息 |
| ❌ | 注册/忘记密码 | 直接砍掉，MVP仅保留测试用户 | - | - |

### 下午（14:00-18:00）：文档编写
| 优先级 | 任务 | 具体操作 | 存放路径 | 验收标准 |
|--------|------|----------|----------|----------|
| ✅ | MINIAUTH+JWT调研文档 | 整理调研结论、选型原因、核心代码示例 | `docs/MINIAUTH+JWT调研结果.md` | 文档结构清晰，可指导开发 |
| ✅ | Stripe集成文档 | 整理集成步骤、密钥配置、核心接口说明 | `docs/Stripe集成方案.md` | 文档可指导部署 |
| ✅ | 架构图定稿 | 用Mermaid Live Editor生成依赖图，导出PNG | `docs/架构依赖图.png`、`README.md` | README包含架构图与项目说明 |

### 当日总结
- 完成标记：□ 是 □ 否
- 问题记录：_______
- 明日调整：_______

---

## 2026.3.6（Day 5）：测试、部署与演示准备
**核心目标**：完成全流程测试，部署至Azure Serverless，准备演示

### 上午（09:00-12:00）：全流程测试与问题修复
| 优先级 | 任务 | 具体操作 | 验收标准 |
|--------|------|----------|----------|
| ✅ | 功能测试 | 1. 未登录访问支付接口/页面返回401；<br>2. 测试支付成功/失败场景；<br>3. 验证JWT过期后无法访问 | 所有核心场景符合预期 |
| ✅ | 兼容性测试 | 用Chrome/Firefox访问，确保页面正常、支付弹窗能唤起 | 主流浏览器兼容 |
| ✅ | 日志检查 | 查看本地日志，确认支付回调、Token生成记录完整 | 日志清晰，便于排查 |

### 下午（14:00-18:00）：Azure部署与演示准备
| 优先级 | 任务 | 具体操作 | 交付物 |
|--------|------|----------|--------|
| ✅ | Azure部署 | 1. 发布`Saas.Infra.MVC`到App Service；<br>2. 在Azure配置环境变量（JWT密钥、Stripe密钥）；<br>3. 开启HTTPS，配置Stripe Webhook指向Azure域名 | 线上可访问地址 |
| ✅ | 线上验证 | 1. 访问线上登录页，完成登录+支付全流程；<br>2. 验证Stripe线上回调 | 线上流程跑通 |
| ✅ | 演示准备 | 1. 编写1分钟演示脚本；<br>2. 整理最终交付物清单 | 演示脚本、交付物包 |

### 当日总结
- 完成标记：□ 是 □ 否
- 问题记录：_______
- 后续迭代规划：_______

---

## 最终交付物清单
1.  **代码交付物**：完整`Saas.Infra.Net`项目代码（可直接编译、部署）
2.  **文档交付物**：`MINIAUTH+JWT调研结果.md`、`Stripe集成方案.md`、`README.md`（含架构图）
3.  **部署交付物**：Azure线上访问地址、部署步骤说明
4.  **演示交付物**：1分钟演示脚本、测试账号（admin/123456）
// R0 占位程序（R5 将替换为 ≤40 行的统一运行时入口）
// R0 placeholder program (R5 replaces this with the ≤40-line unified runtime entry).
System.Console.WriteLine("Quant.Infra.Net.Runtime.Console: scaffold only (R5 ships the real entry).");
